{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | { read -r f; echo \"$f\" | grep -qE '\\.ts$' && npm run build 2>&1 || true; }",
            "timeout": 60,
            "statusMessage": "Building..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint 2>&1",
            "statusMessage": "Running lint..."
          },
          {
            "type": "command",
            "command": "git diff --quiet && git diff --cached --quiet || echo '⚠️  作業後はコミットしてください (uncommitted changes remain)'",
            "statusMessage": "Checking for uncommitted changes..."
          }
        ]
      }
    ]
  }
}
